{
  "name": "openwork-station",
  "version": "0.1.0",
  "private": true,
  "description": "OpenWork Station — an ambient assistant, distributed as an OpenWork App.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "build": "vite build",
    "validate": "openwork-app validate openwork.app.json",
    "package": "node scripts/package.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm validate"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "bun-types": "^1.3.6",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.4"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
